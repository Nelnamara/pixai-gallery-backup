requests
truststore ; python_version >= "3.10"
pillow
PySide6

# gallery server (optional)
flask

# dev / testing
pytest
pytest-mock
pytest-cov   # coverage: python -m pytest --cov=pixai_gallery --cov=pixai_gallery_backup
